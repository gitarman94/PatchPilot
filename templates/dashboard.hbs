<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - PatchPilot</title>
  <link rel="stylesheet" href="/static/styles.css">
  <script defer src="/static/app.js"></script>
</head>
<body>
  {% include "navbar.html" %}

  <h1>Dashboard</h1>
  <p>Total Devices: {{ total_devices }}</p>

  <table id="dashboard-table">
    <thead>
      <tr>
        <th>Hostname</th>
        <th>IP</th>
        <th>OS</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      {% for device in devices %}
        <tr>
          <td>{{ device.hostname }}</td>
          <td>{{ device.ip }}</td>
          <td>{{ device.os }}</td>
          <td>{{ device.status }}</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
</body>
</html>
