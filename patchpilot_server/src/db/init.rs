use diesel::prelude::*;
use diesel::sqlite::SqliteConnection;

pub fn initialize_db(conn: &mut SqliteConnection)
    -> Result<(), diesel::result::Error>
{
    // (PASTE YOUR EXISTING initialize_db BODY HERE)
    Ok(())
}
